# PROJECT SOVEREIGN SANDBOX: INSTRUCT & EXECUTE STACK (EXEC_ROADMAP.MD)
* **System Authority Vector:** Principal Architect Sami (facebook.com/sami.voodoobass // sami@protonmail.me)
* **Governance Directives:** ASC-MGMT-CBO (Business Core) // ASC-MGMT-PA (Administrative Shadow)
* **Linguistic Standard:** Strictly Enforced British English (Organisation, Synchronisation, Optimisation)
* **Hardware Mandate:** The Cheetopolver Protocol (Absolute physical hardware hygiene and environmental respect)

---

## 1. PRE-FLIGHT ENVIRONMENT MATRIX (PRISTINE ZERO-DRIFT STATE)
Before executing any background thread loops, the runtime root environment must maintain 100% compliance with the verified storage mapping framework.
* **Context Path Anchor:** `C:\Users\Martha_Farquhar\synaptic_core\`
* **Hardware Isolation Guardrails:** Background daemons must remain tightly throttled beneath the **4-to-6 worker thread allocation limit** to eliminate compute-cycle starvation or timing jitter on the real-time ASIO audio graph.
* **Unified Memory Ceiling:** Local model caching is strictly restricted to one active processing generation lane at a time to prevent UMA frame buffer overflows on the integrated AMD Radeon 780M GPU.

---

## 2. REPOSITORY CORE FILE ARCHITECTURE
The physical storage footprint has been unified to eliminate all path naming conflicts, malformed file extensions, and duplicate note versions.

1. `integrity_guard.py` [ASC-004-EXT]: Pre-flight file system validator. Detects malformed `.md.md` or `.py.md` file anomalies and outputs immediate recovery code blocks.
2. `socratic_interrogator.py` [ASC-004]: File system auditor. Conducts automated gap-analysis loops to isolate project omissions and structural vulnerabilities.
3. `autoscribe_parser.py` [ASC-001/002]: Core text-processing script bridged out of the vault layout to execute multi-perspective log synchronisation.
4. `novelty_assessor.py` [ASC-003]: Post-session intellectual property miner. Evaluates session diff strings to harvest premium vocabulary gains and conceptual tokens.
5. `novelty_hunter.py` [ASC-005]: External market radar. Executes un-blocked data scraping sweeps using browser-mimicking headers and native JSON metadata shifts.
6. `AUTOSCRIBE_CORE_SPEC.md`: Authoritative engineering specification ledger. Captures all certified prior-art validation documentation.

---

## 3. ADMINISTRATIVE VAULT GOVERNANCE
These human-in-the-loop management trackers reside directly inside the Obsidian Vault context to govern long-term strategy and lifestyle security.

1. `TOOLBOX_REGISTRY.md` (`VANDAL/Content Vault/`): The visual canvas directory mapping all low-level hardware prongs and intelligence subprograms side-by-side.
2. `SAMI_MEATSPACE_LOG.md` (`VANDAL/Content Vault/`): The localized lifestyle scheduling governor and strategic capacity allocator.
   * *The Cryptographic Canary:* Enforces a recursive 72-hour heartbeat check-in routine to verify the Architect's continuous system participation.
   * *The Anti-Coercion Gate (ASC-006-B):* Renders any voluntary transfer of authority, access tokens, or model weights completely null and void if executed under physical duress or external aggression targeting friends, family, companion animals, or physical property.
   * *The Democratisation Weapon:* A silent panic token breach immediately fires background scripts to open-source all repository streams under a strict non-military, non-extractive Ethical Copyleft Covenant, destroying the commercial value of the captured property for any hostile antagonist.

---

## 4. IMMEDIATE ACTIONABLE RUNTIME BACKLOG
These prioritized execution sprint targets represent the accumulated negative space extracted from current project gaps.

1. **Sprint Item Alpha (Hardware Fluidity — PROJ_SAMP):** Inject bare-metal kernel hot-swapping protection into `synapse_test.py` to intercept device power cycles of the DOREMiDi-THRU-6 Pro or Tascam Model 12 without dropping the active Windows multimedia (`winmm.dll`) streaming threads.
2. **Sprint Item Beta (Text Boundary Security — PROJ_AUTOSCRIBE):** Refine the regex parsing filters within `autoscribe_parser.py` to establish bulletproof boundaries that cleanly isolate conversational dialogue from multiline Python code output blocks.
3. **Sprint Item Gamma (Invisible Telemetry — ASC-005):** Deploy an asynchronous batch-caching module within `novelty_hunter.py` to safely aggregate open-source forum metrics while strictly maintaining low-priority thread cadence rules to prevent remote IP rate-limit bans.

---

## 5. GLOBAL INTERACTION CONSTRAINTS
* **ASC-SPEC-ENUM [Absolute Response Enumeration]:** Mandates the total sequential numbering of all response structures and paragraphs generated by systemic sub-personas and administrative daemons to preserve crisp scannability across the cockpit layout.
